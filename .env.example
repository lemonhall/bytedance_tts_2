# 火山引擎 TTS 配置模板
# 复制此文件为 .env 并填入真实的凭证信息

# 火山引擎应用ID
VOLCENGINE_APP_ID=your_app_id_here

# 火山引擎访问令牌
VOLCENGINE_ACCESS_TOKEN=your_access_token_here

# 可选配置
# 默认语音类型
VOLCENGINE_VOICE_TYPE=zh_female_shuangkuaisisi_moon_bigtts

# API版本选择 (v1 或 v3，推荐v3)
TTS_API_VERSION=v3

# V1端点 (兼容性支持)
VOLCENGINE_V1_ENDPOINT=wss://openspeech.bytedance.com/api/v1/tts/ws_binary

# V3端点配置 (推荐使用)
# V3 WebSocket单向流式 (推荐，时延更低)
VOLCENGINE_V3_UNIDIRECTIONAL_WS=wss://openspeech.bytedance.com/api/v3/tts/unidirectional/stream

# V3 WebSocket双向流式
VOLCENGINE_V3_BIDIRECTIONAL_WS=wss://openspeech.bytedance.com/api/v3/tts/bidirection

# V3 HTTP单向流式
VOLCENGINE_V3_UNIDIRECTIONAL_HTTP=https://openspeech.bytedance.com/api/v3/tts/unidirectional

# V3接口类型选择 (unidirectional_ws/bidirectional_ws/unidirectional_http)
TTS_V3_TYPE=unidirectional_ws